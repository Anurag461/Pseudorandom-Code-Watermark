# Same direct numerical dependencies as the existing HF_PARITY_DEPENDENCIES.
# Install upstream separately with --no-deps; see README.md.
torch==2.4.0
transformers==4.51.3
tokenizers==0.21.1
huggingface-hub==0.30.2
safetensors==0.4.5
numpy==1.26.0
scipy==1.14.1
