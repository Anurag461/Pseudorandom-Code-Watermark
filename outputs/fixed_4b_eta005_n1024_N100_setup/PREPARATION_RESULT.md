# Approved CPU preparation completed

- App: [ap-74czeYcCbygVJowk6kuLXP](https://modal.com/apps/new-prc-watermark/main/ap-74czeYcCbygVJowk6kuLXP)
- Worker time: 105.68 seconds; one 4-core CPU worker, 16 GiB RAM, no GPU.
- Provider-reported cost after completion: **$0.00988003** (about one cent).
- Verified original n=T=1024, eta=.05, t=3, r=1014, seed12345 artifact,
  partition fingerprint and all 100 prompt token sequences.
- Downloaded and hash-verified the pinned 4B checkpoint. Verified the cached
  0.6B checkpoint and both model/tokenizer metadata sets.
- Committed the setup and artifact to their existing Modal volumes and retrieved
  hash-verified local copies, request/response, worker log and timing evidence.
- No generations, detector replays, scoring, retries or optional checks ran.

After approximately $0.01946503 of intervening usage by a separate diagnostic
app and this preparation cost, the remaining budget estimate is **$6.49**.
`billing_prepare.json` contains provider billing evidence; accounting may settle
asynchronously. `setup.json` stays byte-for-byte frozen from the reviewed
proposal; subsequent execution status is recorded in `progress.json`.

The next GPU generation stage is awaiting explicit approval. Its reviewed
allowance is $0.90, with an expected $0.24–0.47 cost and 3–6 minute runtime.
