Prepared on `redetection`. **Awaiting user setup review and explicit approval;
no paid computation has launched.** Scope incorporates the user's correction:
eta=0.15 uses the completed **T=6144, N=100 pilot only**. The T4096 cohort and
T4096 reporting point are excluded. Watermarked only; no empirical FPR.

| eta | Saved 8B generation length | Watermarked N | Lengths to report with 0.6B | Points |
|---:|---:|---:|---|---:|
| 0.05 | 1280 | 500 | 1280 down to 848 by 16; also 768, 512, 400, 256, 128 | 33 |
| 0.10 | 3072 | 500 | 3072 down to 1648 by 16 | 90 |
| 0.15 | 6144 | 100 | 6144 only | 1 |

The eta0.05 extra lengths were already scored with 8B in the archived
`comparison-128-256-400-512-768-1024` report. Its candidate file references match
the main eta0.05 cohort; 1024 is already included in the main sweep. The existing
CSV verifies the main 28 and 90 points. For eta0.15, the completed matched
100-record 8B result is 94/100 MAP and 90/100 entropy at 6144. No lower length
was scored for this matched pilot. Do not substitute the separate 500-record
4096 result or rerun the previous pilot's stopped independent reference pass.

Recover 0.6B probabilities once at each cohort's full length, then use exact
causal prefixes for all requested lengths. This is 1,100 saved completions,
2,789,300 recovered probabilities and 123,200 detector decisions across 124
length/eta points. MAP and entropy share the same recovered trace. CPU scoring
uses the existing `online_prc_redetection.prefix_scores` helper and preserves
the existing summation order. Score the complete frozen list, without the
earlier campaign's stop-at-MAP-below-90% rule. Prefixes require no additional
redetection, but CPU scoring has the separate estimated charges below.

**New generations: 0. Generation cost: $0. Null generations/replays: 0.
Separate null cost: $0. Empirical FPR: not evaluated.** The target FPR remains
0.001 under the original online `one_shot` policy at each reported length.

Reuse the archived online keys, two-bucket partition, prompts, original token
IDs, manifests and saved 8B score reports. Read-only Modal listings confirmed
all 500/500/100 watermarked records and matched the cloud manifests against
local copies. The T6144 source artifact hash is also verified. The original
T6144 tokens were previously generated by extending the earlier saved prefix;
this proposal consumes the complete saved T6144 records and performs no
generation or continuation. The known earlier 8B→0.6B completion-only trace is
only T640 and cannot replace the full T1280 recovery. Existing 8B detection
traces and generation-time probabilities cannot supply 0.6B probabilities.

Detector: **Qwen/Qwen3-0.6B-Base**, model/tokenizer revision
`da87bfb608c14b7cf20ba1ce41287e8de496c0cd`. The saved 8B comparison uses
**Qwen/Qwen3-8B-Base**, checkpoint revision
`49e3418fbbbca6ecbdf9608b4d22e5a407081db4`. Both current cached tokenizer.json
files were downloaded read-only and have identical SHA-256
`c0382117ea329cdf097041132f6d735924b697924d6f6fc3945713e96ce87539`.
Both model configs have 151,936 vocabulary rows. Token-ID replay is directly
compatible; there is no text round-trip. Exact 0.6B weights and metadata hashes
are frozen in [setup.json](setup.json); CPU preparation rechecks the cache and
stops if a file is absent or changed. No checkpoint downloads are proposed.

Protocol: **completion_only_raw_abstain_v1**, raw completion IDs without prompt,
BOS/EOT or other special prefix; first-coordinate abstention; coordinates 2..T
recovered by 0.6B; MAP and entropy weights. BF16 inference, TF32 disabled, static
KV cache, float32 stored probabilities and float64 CPU scoring. Use seed12345,
t=3, online row rate99/100 with `rational_round_half_even_v1` startup clamping,
and the original `hmac_sha256_rejection_v1` supports. Reuse partition SHA-256
`503d1cf93958f0d765606ed3e25aa87a1a777cd08d8f44436b0c6ae9716aa184`.
Original generation sampling is unchanged because no tokens are generated.
Generation-time `p_trace` is never used for completion-only detection scores.

There are **nine separately named paid runs**, sequentially, with one worker
at a time. Every run reserves and limits CPU to four physical cores. Each GPU
run loads 0.6B once and processes its listed batches sequentially, saving each
primary trace immediately. No benchmarks, automatic retries, reference replays
or extra paid validation passes are included.

| Named run | Resource / host RAM | Batching / workload | Expected time | Expected cost | Allowance |
|---|---|---|---|---:|---:|
| eta005 prepare | CPU / 16 GiB | Verify and freeze 500 records | 1–2.5 min | $0.006–0.015 | $0.025 |
| eta005 replay | A100 80GB / 16 GiB | 4 × 125 at T1280 | 5–8 min | $0.24–0.38 | $0.55 |
| eta005 score | CPU / 8 GiB | 500 × 33 lengths × 2 weights | 0.25–2 min | $0.002–0.009 | $0.035 |
| eta010 prepare | CPU / 16 GiB | Verify and freeze 500 records | 1–2.5 min | $0.006–0.015 | $0.025 |
| eta010 replay | A100 80GB / 16 GiB | 5 × 100 at T3072 | 23–32 min | $1.08–1.50 | $1.75 |
| eta010 score | CPU / 8 GiB | 500 × 90 lengths × 2 weights | 0.25–2 min | $0.002–0.009 | $0.035 |
| eta015 prepare | CPU / 16 GiB | Verify and freeze 100 records | 1–2.5 min | $0.006–0.015 | $0.025 |
| eta015 replay | A100 80GB / 16 GiB | 2 × 50 at T6144 | 20–30 min | $0.94–1.40 | $1.75 |
| eta015 score | CPU / 8 GiB | 100 × 1 length × 2 weights | 0.25–2 min | $0.002–0.009 | $0.035 |

Expected total: approximately **$2.30–$3.40 and 60–90 minutes elapsed**,
excluding approval waits and provider queues. Per-run allowances total $4.23;
add $0.07 startup reserve for a **$4.30 conservative total**. These are estimates,
not a provider-enforced dollar cap. CPU preparation work timeouts are180s;
scoring timeouts300s; GPU timeouts600s/2100s/2100s. Function timeout adds30s,
startup is bounded to30s, and idle shutdown is2s. A failed or timed-out run
stops; any retry or replacement needs a new estimate and explicit approval.

Cost basis: existing successful A100 0.6B normal batches took about55s at
T1024/batch125, 189–198s at T2048/batch125, and 564–573s at T4096/batch100.
Full-reference batches are explicitly excluded from these timing estimates.
Interpolate/extrapolate token-step attention work by batch size and squared
sequence length, keeping model-loading overhead. The T6144/batch50 shape has
307,200 sequence-tokens versus409,600 in the established T4096/batch100 reference
that peaked at51.64GB live GPU allocation; memory is projected below that
reference. Its exact runtime and peak are estimates, not benchmark results.
The T4096 timing reference is an earlier **0.6B-generated, 0.6B-detected** run,
not the excluded eta0.15 8B cohort. Saved references are in
[timing_references.json](timing_references.json).

Read-only billing shows an estimated remaining balance of **$5.59410451**.
The expected spend would leave about$2.19–$3.29; the conservative allowance
would leave **$1.29410451**. This balance derives from the user's approximate
starting balance and recorded provider usage, not a guaranteed credit balance.
Recheck billing before launches. Evidence: [billing_before_setup.json](billing_before_setup.json).

The new narrow stage driver is `online_8b_to_0p6b.py`. It reuses the existing
preparation, Qwen loading, completion replay, prefix scoring and CSV routines.
The existing runtime files were not changed. The corrected scientific/native
imports-before-NumPy-alias loading order is retained. Four metadata-only guard
tests passed, covering exact source hashes, N=100 at6144, rejection of4096,
approval/attempt guards and changed-manifest refusal. No model execution or
experiment scoring ran locally. Exact execution source snapshots remain local.

After approval, collect and narrowly commit each primary trace before scoring,
preserve unrelated working-tree changes, and never push. Cache source refs,
prompt/key artifacts, prepared tokens, manifests, traces, scores, timing and
itemized provider costs. Add124 0.6B rows to the existing CSV with N=500/N=100
explicit and empirical FPR skipped. Report paired saved 8B and new0.6B results
separately from historical prompted TPR; do not mislabel 8B scores as old0.6B
scores. No extra reproducibility archive upload is proposed.

Approved-stage invocation (not yet authorized or executed):

```sh
MODAL_PROFILE=new-prc-watermark /opt/anaconda3/bin/python -m modal run \
  online_8b_to_0p6b.py --case eta005_T1280_N500 --stage prepare \
  --approval-reference 'ACTUAL EXPLICIT USER APPROVAL FOR THE LISTED RUN'
```

The other case IDs are `eta010_T3072_N500` and `eta015_T6144_N100`; stages are
`prepare`, `replay`, `score`. Repeat only storage collection, if needed, with
`python online_8b_to_0p6b.py collect CASE STAGE`; never repeat a paid attempt.
